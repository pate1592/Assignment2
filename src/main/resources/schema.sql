CREATE TABLE pet (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         pet_name VARCHAR(30),
                         pet_kind VARCHAR(30),
                         Pet_Gender VARCHAR(30),
                         Vaccinated BOOLEAN,
);
